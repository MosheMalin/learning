{
  "name": "learning",
  "private": true,
  "description": "Family learning apps - see english-words/README.md",
  "scripts": {
    "test": "node --test tests/tracker/*.test.js && playwright test",
    "test:e2e": "playwright test",
    "test:ui": "playwright test --ui",
    "test:tracker": "node --test tests/tracker/*.test.js",
    "serve": "python serve.py english-words 8123"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0"
  },
  "workspaces": [
    "shared/*"
  ]
}
